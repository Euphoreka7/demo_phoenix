<div class="col-xs-10 col-xs-offset-1">
  <h2>Edit Quote</h2>

  <%= render "form.html", changeset: @changeset,
                          action: quote_path(@conn, :update, @quote) %>

  <%= link "Back", to: quote_path(@conn, :index) %>
</div>
